/**
 * これはMinePaintのコンフィグファイルです。
 * ここの項目を書き換えることで仕様を変更することが出来ます。
 *
 * This is MinePaint's config file.
 * You can change the specifications by rewriting the elements here.
 */

export const config = {
	buildNumber: 4725,
	opTag: "admin"
};
